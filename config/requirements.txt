# ============================================
# IntegrityX - Financial Document Integrity System
# Requirements file for the complete application
# ============================================

# Environment & Configuration
python-dotenv>=1.0.0

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # Required for FastAPI Form data handling
sqlalchemy>=2.0.0
alembic>=1.12.0

# Walacor SDK (FIXED VERSION)
walacor-python-sdk>=0.1.5  # Note: v1.0.0 does not exist, use 0.1.5

# Data Processing & Utilities
pathlib2>=2.3.7
typing-extensions>=4.0.0
jsonschema>=4.0.0
canonicaljson>=1.6.0

# Machine Learning & Analytics
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
joblib>=1.3.0  # For model persistence

# Security & Cryptography
argon2-cffi>=23.1.0  # Quantum-safe password hashing
cryptography>=41.0.0  # General cryptography operations
pycryptodome>=3.18.0  # Additional crypto functions

# AWS SDK for S3 presigned URLs
boto3>=1.34.0
botocore>=1.34.0

# HTTP client for health checks
aiohttp>=3.9.0

# Cache & Performance
redis>=5.0.0
hiredis>=2.2.0  # C parser for Redis (faster)

# Monitoring & Metrics
prometheus-client>=0.19.0
prometheus-fastapi-instrumentator>=6.1.0

# System Monitoring (optional but recommended)
psutil>=5.9.0  # For memory/disk monitoring